# CLAUDE.md

> **No rules live here — this is a pointer, not a copy.** The single source of always-on agent
> rules is **[AGENTS.md](AGENTS.md)**; read it first and follow it. Do not restate rules in this
> file — change `AGENTS.md` instead.

- The harness map (rules, commands, hooks, gates, skills) is
  [`docs/AI_DEVELOPMENT.md`](docs/AI_DEVELOPMENT.md); the harness bodies live under `.cursor/`
  and are binding for Claude Code too — load the `.cursor/rules/*.mdc` matching the files you
  touch (see AGENTS.md for the scope table).
- The engine contract is [`docs/SPECIFICATION.md`](docs/SPECIFICATION.md); backlogs are
  `docs/ROADMAP.md` (`R-xx`) and `docs/DOCS_SITE_ROADMAP.md` (`D-xx`).
